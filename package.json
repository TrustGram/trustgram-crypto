{
  "name": "trustgram-crypto",
  "version": "0.1.0",
  "type": "module",
  "exports": "./dist/crypto.js",
  "files": ["dist"],
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=esm --sourcemap --outfile=dist/crypto.js",
    "dev": "esbuild src/index.ts --bundle --format=esm --sourcemap --outfile=dist/crypto.js --watch",
    "prepare": "npm run build",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "esbuild": "^0.21.0",
    "serve": "^14.0.0",
    "monocart-reporter": "^2.0.0"
  }
}
